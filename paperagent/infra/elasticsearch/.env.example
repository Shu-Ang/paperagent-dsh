# Pin the Elastic Stack version. Do not use the mutable "latest" tag.
STACK_VERSION=8.19.20

# Required before the first start. Use a unique password; this file is safe to commit,
# but infra/elasticsearch/.env is ignored and holds the real local value.
ELASTIC_PASSWORD=replace-with-a-unique-local-password
