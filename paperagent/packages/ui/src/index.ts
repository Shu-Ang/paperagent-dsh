/** Host half only exists so DSH discovers the package's browser contribution. */

export const name = 'paperagent-ui'

export function apply(): void {
  // All visible behavior is registered by ./client in the browser runtime.
}
